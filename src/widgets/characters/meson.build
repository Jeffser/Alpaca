moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'alpaca' / 'widgets' / 'characters'

blocks = [
  '__init__.py',
  'character_page.py',
  'export_page.py'
]

install_data(blocks, install_dir: moduledir)