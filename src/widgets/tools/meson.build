moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'alpaca' / 'widgets' / 'tools'

blocks = [
  '__init__.py',
  'tools.py',
  'manager.py',
  'selector.py'
]

install_data(blocks, install_dir: moduledir)