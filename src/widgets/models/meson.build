moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'alpaca' / 'widgets' / 'models'

models = [
  '__init__.py',
  'added.py',
  'pulling.py',
  'creator.py',
  'common.py',
  'basic.py',
]

install_data(models, install_dir: moduledir)