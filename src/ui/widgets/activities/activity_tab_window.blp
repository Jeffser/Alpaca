using Gtk 4.0;
using Adw 1;

template $AlpacaActivityTabWindow: Adw.ApplicationWindow {
  title: _("Activities");
  content: $AlpacaActivityManager activity_manager {
    name: "reattachable";
  };
  close-request => $close();
}
